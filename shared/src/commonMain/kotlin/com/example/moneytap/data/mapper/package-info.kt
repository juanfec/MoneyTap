/**
 * Data Mappers
 *
 * This package contains mappers that convert between DTOs and domain models.
 *
 * Guidelines:
 * - Use extension functions for clean mapping syntax
 * - Handle null values and defaults appropriately
 * - Keep mappers pure (no side effects)
 * - Create bidirectional mappers when needed (toDto, toDomain)
 */
package com.example.moneytap.data.mapper
