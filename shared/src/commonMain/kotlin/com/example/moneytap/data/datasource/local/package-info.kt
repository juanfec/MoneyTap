/**
 * Local Data Sources
 *
 * This package contains local data source implementations (database, preferences, cache).
 *
 * Guidelines:
 * - Use SQLDelight for database operations
 * - Use expect/actual for platform-specific storage (SecureStorage)
 * - Handle data persistence and caching
 * - Never expose database entities directly; use DTOs
 */
package com.example.moneytap.data.datasource.local
